---
title: 2024 CUMCM B题阶段3算法设计与条件化求解工程报告
problem: 2024-CUMCM-B
stage: 3-algorithm-design
status: q1-restructured
q1_restructured_at: "2026-08-30 03:55:51 +08:00"
design_policy: effect-first-tool-unconstrained
implementation_language: Python-primary-with-certified-tool-extensions
model_report: 国赛练习题/CUMCM2024Problems/B题/模型设计/B题阶段2模型设计报告.md
evidence_report: 国赛练习题/CUMCM2024Problems/B题/模型设计/B题阶段2模型论文依据与详细介绍.md
locked_models:
  - Q1-M6
  - Q2-M2
  - Q3-M3
  - Q4-M2
  - Q4-M3
locked_algorithms:
  - Q1-A2
  - Q2-A1
  - Q3-A1
  - Q4-A1
  - Q4-A2
next_stage: 4-validation-planning
---

# B题阶段3算法设计与条件化求解工程报告

关联：[[国赛练习题/CUMCM2024Problems/B题/模型设计/B题阶段2模型设计报告.md|阶段2模型设计报告]] · [[国赛练习题/CUMCM2024Problems/B题/模型设计/B题阶段2模型论文依据与详细介绍.md|模型与算法论文依据]] · [[国赛练习题/CUMCM2024Problems/B题/模型设计/B题阶段2模型确认单.md|阶段2确认单]] · [[国赛练习题/CUMCM2024Problems/B题/B题.pdf|题目原文]]

确认入口：[[国赛练习题/CUMCM2024Problems/B题/算法设计/B题阶段3算法确认单.md|阶段3算法确认单]]

> [!warning] 流程图版本说明
> 原 SVG 含旧 Q1-M5/A1 流程，Q1 重构后不再引用；现行 Q1 执行契约以本报告第4节为准。Q2–Q4 的文字算法契约不受影响。

> [!important] 本阶段边界
> 阶段2已经回答“优化什么”；本报告只回答“怎样可靠地计算”。本阶段建立算法契约、工具栈、复杂度、误差控制、日志和失败路径，不提前伪造 Q1–Q4 数值结果，也不进入阶段6代码实现。

> [!success] 效果优先原则
> 用户明确要求：**不以当前工具是否已安装作为算法降级理由，以结果质量、精确性和可核验性优先，并把所需工具写入设计方案。** 因而主线保留完整策略枚举、稀疏精确评价、全策略后验比较和稳健搜索认证；工具缺失只形成部署任务，不改变算法结论。

## 1. 已确认模型—算法接口

| 问题 | 模型规定 | 本阶段算法职责 | 不得混淆 |
|---|---|---|---|
| Q1 | Q1-M6：在 AQL/LTPD 双风险约束下最小化灰区最坏 ASN | Q1-A2 求精确二项基线，以截尾 SPRT 生成边界并用路径 DP 校准风险 | SPRT/DP 是求解算法，$p_1$ 与风险约束属于模型 |
| Q2 | Q2-M2：吸收 Markov 奖励闭环利润优化 | Q2-A1 生成策略状态链、检查吸收性、求奖励方程并比较 16 种策略 | 枚举和线性求解不是模型 |
| Q3 | Q3-M3：装配树—吸收 Markov 闭环利润优化 | Q3-A1 缓存树状态核、精确评价 65536 种策略 | 树和物料守恒属于模型，后序遍历属于算法 |
| Q4 | Q4-M2 后验期望利润；Q4-M3 矩形集 max–min | Q4-A1 后验 MC；Q4-A2 联合构集、单调性认证与最坏情形搜索 | 后验/不确定集进入模型，抽样和搜索属于算法 |

算法选择遵循五条门禁：

1. **有限策略空间优先精确搜索**，不以 GA/SA 替代可完成的全枚举；
2. **每次策略评价必须可复核**，利润同时输出采购、检测、装配、拆解、调换和重生产分账；
3. **统计误差与数值误差分开**，Q1 精确尾概率/路径风险、Q4 CS 覆盖、Monte Carlo 误差和线性方程残差分别记录；
4. **失败关闭（fail closed）**，非吸收、病态、未收敛、超时不能被当作普通有限利润；
5. **最优性称谓与证据一致**：全枚举完成才称“固定策略域精确最优”；稳健内层未认证时只称“数值最坏解”。

---

## 2. 统一输入、输出与代码接口

### 2.1 输入数据结构

#### Q1 抽样流

```text
SampleRecord:
  item_id: str
  t: int
  is_defective: bool
  batch_id: str | null
  observed_at: datetime | null
```

算法只需要累计量 $(t,K_t)$，其中 $K_t$ 为前 $t$ 件中的次品数；原始记录仍需保存，以审计重复、漏记和停止后的违规追加观测。

#### Q2 六种情形

```text
scenario_id,
p1, buy_cost1, test_cost1,
p2, buy_cost2, test_cost2,
pf, assembly_cost, final_test_cost, sale_price,
replacement_loss, disassembly_cost
```

要求概率在 $[0,1]$，成本非负，货币单位统一为元/件。

#### Q3 装配树

```text
Node:
  node_id: str
  node_type: leaf | assembly | root
  defect_rate: float
  buy_cost: float | null
  test_cost: float
  assembly_cost: float | null
  disassembly_cost: float | null

Edge:
  parent_id: str
  child_id: str
  quantity: int = 1
```

根节点另存 `sale_price` 与 `replacement_loss`。载入后必须检查有且仅有一个根、无有向环、每个非根节点只有一个父节点，以及父节点的直接子件集合与题图一致。

#### Q4 抽样证据

为避免与检测决策 $x_v$ 混淆，抽样证据统一记为 $(N_j,K_j)$：

```text
SamplingEvidence:
  parameter_id: str
  N_j: int
  K_j: int
  sampled_object: str
  conditioning: str
  stopping_rule: str
  batch_id: str | null
```

内部节点的 $K_j/N_j$ 必须对应“直接子件均合格条件下的装配失败率”；若条件不符，算法返回 `INVALID_CONDITIONING`，禁止直接代入。

### 2.2 统一输出对象

每个策略评价统一输出：

```text
PolicyEvaluation:
  problem_id: Q2 | Q3
  scenario_id: str
  policy_bits: tuple[int, ...]
  status: str
  expected_profit: float | null
  expected_total_cost: float | null
  cost_breakdown: dict[str, float]
  event_counts: dict[str, float]
  absorption_margin: float | null
  linear_residual: float | null
  runtime_ms: float
  state_count: int
  warnings: list[str]
```

Q1 额外输出固定样本基线、二元序贯边界、双风险、OC/ASN/停止分位数和 $p_1$ 敏感性；Q4 额外输出后验均值、区间、最优概率、遗憾、MC 误差、最坏参数位置及稳健搜索界。

### 2.3 计划代码模块

```text
B题/代码/
├── config/
│   ├── q1_design.yaml
│   ├── solver_tolerances.yaml
│   └── toolchain.yaml
├── common/
│   ├── schemas.py
│   ├── logging_utils.py
│   ├── numerical_checks.py
│   └── reproducibility.py
├── q1/
│   ├── fixed_binomial_plan.py
│   ├── sprt_boundaries.py
│   ├── exact_path_dp.py
│   ├── asn_supremum.py
│   └── risk_calibration.py
├── production/
│   ├── tree_schema.py
│   ├── state_kernel.py
│   ├── absorption.py
│   └── reward_solver.py
├── q2/exact_search.py
├── q3/tree_exact_search.py
├── q4/
│   ├── posterior_mc.py
│   ├── simultaneous_cs.py
│   ├── monotonicity_certificate.py
│   └── robust_search.py
└── tests/
```

这里只规定未来实现边界；阶段6才创建实际代码。

---

## 3. 效果优先工具栈

### 3.1 主工具

| 工具 | 用途 | 必要性 | 设计说明 |
|---|---|:---:|---|
| Python 3.10 独立环境 | 主求解、SciPy 精确尾概率与 `confseq` 兼容环境 | 必需 | Q1 主线使用 SciPy；`confseq` 只服务 Q4 simultaneous CS/诊断。当前 Python 3.13 可保留为编排环境 |
| `confseq` | Q4 simultaneous Bernoulli CS 与 Q1 旧模型诊断复核 | Q4 必需、Q1 非必需 | 不再生成 Q1-M6 主边界；Q4 使用官方实现并以独立公式交叉核验 |
| NumPy / SciPy | 数组、特殊函数、稀疏矩阵、LU、特征值、全局优化 | 必需 | `scipy.sparse`、`splu/spsolve`、`eigs`、`optimize.shgo`、`differential_evolution` |
| JAX + CUDA/TPU 后端 | 自动微分、批量利润、Jacobian 符号检查与加速器并行 | 必需 | Q4 单调性反例搜索和批量评价；只负责数值检查，不替代区间证明 |
| pandas / PyArrow | 表 1、表 2、策略结果和批次 MC 结果 | 必需 | 中间结果存 Parquet，避免 65536 策略的 CSV 类型丢失 |
| Zarr | Q4 大型“后验样本×策略”利润数组 | 推荐 | 按样本块和策略块写共享对象存储，避免一次装入内存，并支持最终精确分位数复算 |
| NetworkX | 可达图、强连通分量和闭类检测 | 必需 | 用图论吸收性检查与谱半径互证 |
| Numba | 状态核生成、策略位运算和批量评价加速 | 必需 | 不改变数学算法，只降低循环开销 |
| Ray 或 Dask Distributed | Q3 全策略和 Q4 全策略×后验样本分布式计算 | 必需 | 固定任务切片与确定性归并，不按完成先后改变结果 |
| Joblib | 单机并行回退 | 推荐 | Q2/Q1 或小规模调试使用 |
| mpmath | 高精度对照、病态矩阵与边界根求解复核 | 推荐 | 只对少量疑难策略启用 |
| Pydantic / PyYAML | 输入 schema 与配置冻结 | 推荐 | 参数、容差和版本写入运行清单 |
| pytest / Hypothesis | 单元测试与性质测试 | 必需 | 极端概率、物料守恒、状态编码和随机种子复现 |
| uv/Conda + Docker/Apptainer | 隔离环境与集群复现 | 推荐 | 固化 Python、Julia、系统库和锁文件，避免节点版本漂移 |

Q1 的二项尾概率、似然比边界和 DP 以 SciPy/独立 log-PMF 复核为主；官方 ConfSeq 的 `beta_binomial_*` 与 `bernoulli_confidence_interval` 仅用于 Q4 simultaneous CS 和历史诊断：[ConfSeq 官方仓库](https://github.com/gostevehoward/confseq/)。稀疏求解以 SciPy 官方接口为准：[SciPy sparse](https://docs.scipy.org/doc/scipy/reference/sparse.html)。

### 3.2 严格稳健认证扩展

| 工具 | 用途 | 启用条件 |
|---|---|---|
| Julia `IntervalArithmetic.jl` + `IntervalOptimisation.jl`，或 IBEX | 区间分支定界、给 Q4 内层最小化提供上下界 | 需要宣称连续不确定集内的严格稳健最优时启用 |
| Julia `IntervalArithmetic.jl`/Arb 或等价 Bernstein 区间工具 | 认证 Q1 在 $p\in[p_0,p_1]$ 上的 ASN 上确界 | 需要返回 `SUCCESS_EXACT_FAMILY` 时启用；否则只能标记网格目标 |
| `scipy.optimize.shgo` | 确定性全局搜索候选与局部极小点集合 | 单调性不能证明时主搜索 |
| `scipy.optimize.differential_evolution` | 独立随机全局搜索互证 | 与 SHGO 结果不一致或目标不光滑时启用 |
| Mermaid CLI | 算法流程与论文图示 | 文档阶段使用 |

`differential_evolution` 只提供数值搜索结果和运行状态，不等于严格全局证书；其迭代、种群和停止条件必须完整记录。[SciPy differential_evolution](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.differential_evolution.html)

### 3.3 计算资源

| 任务 | 推荐资源 | 原因 |
|---|---|---|
| Q1 断点校准 | 普通工作站可运行；16 核、32 GB 推荐 | $p_0,p_1$ 端点 DP 先剪枝，多个 $N_{\max}$/边界断点可确定性并行 |
| Q2 | 普通工作站 | 仅 16 个策略，重在状态正确性 |
| Q3 | 32–64 核、128 GB，或 Ray 集群 | 全部 65536 策略，不用元启发式替代 |
| Q4-Q2 | 16–32 核 | 16 策略×后验批次 |
| Q4-Q3 | Ray 管理的多 GPU/TPU + CPU 混合集群、共享对象存储 | GPU/TPU 承担 JAX 批量利润评价，CPU 节点承担图检查、稀疏/区间认证；全策略×后验样本是主要瓶颈 |

---

## 4. Q1-A2：精确二项基线 + 风险校准截尾 SPRT + 精确路径 DP

> [!important] 重构口径
> Q1-M6 规定的是“两质量点、双风险约束下尽可能少抽检”的优化问题；Q1-A2 才是求解流程。旧 Q1-A1 的双单侧 CS 针对单阈值任意停止推断，不能继续充当新模型的主算法。旧代码与旧结果必须标记为 `STALE_MODEL_RESULT`，不得只换标题后复用。

### 4.1 输入、输出与可行性契约

| 类别 | 内容 |
|---|---|
| 设计输入 | $p_0=0.10$；$p_1\in\{0.12,0.13,0.15,0.20\}$，主情景 $p_1=0.13$；$\alpha=0.05$；$\beta=0.10$；截尾倍率 $\kappa\in\{1,1.25,1.5,2\}$ |
| 数据假设 | $X_i\overset{\mathrm{i.i.d.}}{\sim}\operatorname{Bernoulli}(p)$；检测完美、无损；批内参数恒定 |
| 决策输出 | 固定样本基线 $(n_F,c_F)$；序贯边界 $(a_t,r_t)$；截尾 $N_{\max}$；终端截断数 $c_N$ |
| 性能输出 | 两端点精确风险、OC 曲线、灰区 ASN 上确界及位置、P50/P90/P99、最大样本量、相对固定基线的 ASN 节省率 |
| 硬门禁 | $P_{p_0}(R)\le0.05$，$P_{p_1}(A)\le0.10$；$N_{\max}\le\kappa n_F$；每条路径二元终止：$P_p(A)+P_p(R)=1$ |
| 最优性口径 | 边界族搜索与 $p\in[p_0,p_1]$ ASN 上确界都完成认证时称“族内最优”；否则按证据降为网格内/局部可行 |

`p1` 不是由样本反推的自由变量。若把它与边界一起优化，算法会倾向选择更远、更容易区分的劣质点，从而用较少样本换取过宽容的质量要求。因此只能把 $p_1$ 当工程输入并做敏感性分析。

### 4.2 最小固定样本精确二项基线

固定抽样方案在第 $n$ 件后统计次品数 $K_n$，当 $K_n\le c$ 时接收，否则拒收。两端点风险为

$$
\alpha_F(n,c)=P_{p_0}(K_n>c)
=1-F_{\operatorname{Bin}(n,p_0)}(c),
$$

$$
\beta_F(n,c)=P_{p_1}(K_n\le c)
=F_{\operatorname{Bin}(n,p_1)}(c).
$$

按 $n=1,2,\ldots$ 递增搜索。对每个 $n$，先取使 $\alpha_F\le0.05$ 的最小整数 $c$，再检查 $\beta_F\le0.10$；首次通过即为 $(n_F,c_F)$。主计算使用 `scipy.stats.binom.sf/cdf`，不得用 `1-cdf` 计算极小上尾；再以 log-PMF 累加或 `mpmath` 独立复核。

以下只是**固定基线单元测试 oracle**，不是新序贯方案的最终结果：

| $p_1$ | $n_F$ | $c_F$ | $\alpha_F$ | $\beta_F$ |
|---:|---:|---:|---:|---:|
| 0.12 | 2096 | 232 | 0.0493657550 | 0.0995398380 |
| 0.13 | 968 | 112 | 0.0486497073 | 0.0997558579 |
| 0.15 | 368 | 46 | 0.0496448526 | 0.0999328161 |
| 0.20 | 109 | 16 | 0.0432080762 | 0.0990770489 |

实现若不能在 $10^{-9}$ 内复现该表，禁止进入序贯优化。Duan & Zhang（2026）的 $n=271/165$ 是单侧筛查规模，其表4在 $p=0.13$ 的拒收率仅约 47%；这类方案必须在本门禁下判定为“不满足 $\beta=0.10$”，不能误作双风险基线。

### 4.3 Wald SPRT 仅用于生成初始边界

针对简单端点 $H_0:p=p_0$ 与 $H_1:p=p_1$，累计对数似然比为

$$
\ell_t
=K_t\log\frac{p_1}{p_0}
 +(t-K_t)\log\frac{1-p_1}{1-p_0}.
$$

Wald 初始阈值取

$$
A_0=\frac{\beta}{1-\alpha},
\qquad
B_0=\frac{1-\beta}{\alpha},
\qquad A_0<1<B_0.
$$

于是 $\ell_t\le\log A_0$ 时接收，$\ell_t\ge\log B_0$ 时拒收，其余继续。令

$$
d=\log\frac{p_1(1-p_0)}{p_0(1-p_1)},
\qquad
b=\log\frac{1-p_1}{1-p_0},
$$

则初始整数边界为

$$
a_t^{(0)}=\left\lfloor\frac{\log A_0-tb}{d}\right\rfloor,
\qquad
r_t^{(0)}=\left\lceil\frac{\log B_0-tb}{d}\right\rceil.
$$

Wald 阈值忽略离散越界和有限截尾效应，**不能直接当作风险已经达标**。代码必须在精确 DP 后校准；凡仅输出 Wald 近似风险的实现都返回 `RISK_NOT_EXACTLY_VERIFIED`。

### 4.4 强制二元截尾与精确路径 DP

对 $t<N_{\max}$，候选规则由接收边界 $a_t$ 和拒收边界 $r_t$ 决定；到达 $N_{\max}$ 后，用整数 $c_N$ 强制终止：

$$
K_{N_{\max}}\le c_N\Rightarrow A,
\qquad
K_{N_{\max}}>c_N\Rightarrow R.
$$

因此不存在未决状态。对任意真实次品率 $p$，令 $q_t(k;p)$ 为时刻 $t$ 决策后仍继续且累计 $k$ 个次品的概率质量，$q_0(0;p)=1$。到达下一状态前

$$
\widetilde q_{t+1}(k;p)
=(1-p)q_t(k;p)+p\,q_t(k-1;p).
$$

按照边界把 $\widetilde q_{t+1}$ 分流到 $A$、$R$ 或下一层继续状态；在 $N_{\max}$ 按 $c_N$ 全部分流。由此精确得到

$$
P_p(A),\quad P_p(R),\quad
\operatorname{ASN}(p)=\sum_{t=0}^{N_{\max}-1}\sum_kq_t(k;p),
$$

以及停止时间分布和分位数。概率守恒要求

$$
P_p(A)+P_p(R)=1\pm10^{-12}
$$

（在极端长边界下可放宽到 $10^{-10}$，但必须记录）。性能评价禁止用 Monte Carlo 替代此有限状态递推。

### 4.5 风险校准与灰区最坏 ASN 优化

候选规则写为

$$
\pi(N_{\max},c_N,h_A,h_R),
$$

其中 $h_A<h_R$ 是校准后的对数似然阈值；Wald 的 $(\log A_0,\log B_0)$ 只是搜索起点。阈值只在越过某个可达状态分数 $\ell_t(k)$ 时改变整数动作表，因此搜索应在这些**离散断点**上进行，而不是对无效的小数网格盲扫。

对每个 $(p_1,\kappa)$ 执行：

1. 求精确固定基线 $(n_F,c_F)$，把“直到 $n_F$ 才按 $c_F$ 决策”加入 $\kappa\ge1$ 的候选集，保证至少有一个可行方案；
2. 以 Wald 边界为中心生成可达 LLR 断点，按风险单调方向扩展 $h_A,h_R$；
3. 对 $N_{\max}=1,2,\ldots,\lfloor\kappa n_F\rfloor$ 与可行 $c_N$ 做分层搜索；
4. 先用端点 DP 计算 $\alpha(\pi)=P_{p_0}(R)$、$\beta(\pi)=P_{p_1}(A)$，不达双风险门禁者立即剪枝；
5. 对可行候选计算
   $$
   J(\pi)=\sup_{p\in[p_0,p_1]}\operatorname{ASN}(p;\pi);
   $$
6. 以 $J$ 最小为主，依次以更小 $N_{\max}$、更小端点最大 ASN、端点平均 ASN 破同；
7. 在完整 OC 网格重新核验单调性和风险，最后才写入成功状态。

#### 灰区 ASN 上确界认证

固定边界后，存活到时刻 $t$ 且累计 $k$ 个次品的路径条数是确定整数，因此 $\operatorname{ASN}(p;\pi)$ 是次数不超过 $N_{\max}$ 的 Bernstein 型多项式。上确界搜索不能只看 $p_0,p_1$。至少执行：

1. 首批点包含 $p_0,p_1$ 与零漂移点
   $$
   p^\dagger=-\frac{b}{d},
   $$
   其中 $b,d$ 见 4.3；
2. 在 $[p_0,p_1]$ 自适应细分，用高精度函数值和 Bernstein/区间包络给每个子区间的 ASN 上下界；
3. 直到全局上界与当前最好下界之差
   $$
   U_J-L_J\le\epsilon_{\rm ASN},
   \qquad
   \epsilon_{\rm ASN}=\max(10^{-6},10^{-8}n_F);
   $$
4. 保存最大点区间、上下界和细分日志。

完成“全部预声明边界断点 + 灰区上确界认证”才返回 `SUCCESS_EXACT_FAMILY`。若边界只在 Wald 邻域局部搜索，返回 `SUCCESS_LOCAL_CALIBRATION`；若边界搜索完整但灰区只做离散网格，返回 `SUCCESS_GRID_OBJECTIVE`。后两者都不能写“族内最优”。

### 4.6 $p_1$—截尾倍率双重敏感性

独立运行

$$
p_1\in\{0.12,0.13,0.15,0.20\},
\qquad
\kappa\in\{1,1.25,1.5,2\}.
$$

主结果固定 $p_1=0.13,\kappa=1$。$p_1=0.13$ 是 Duan & Zhang（2026）的可行动恶化基准；0.12 检查更严格分辨要求，0.15/0.20 检查放宽 LTPD 后的省样本效果。$\kappa$ 则量化“最大可能抽检数”与“灰区最坏 ASN”的交换。每档必须报告：

- 固定 $(n_F,c_F)$ 与序贯 $(N_{\max},a_t,r_t,c_N)$；
- $\alpha,\beta$ 的精确值及风险裕度；
- 灰区 ASN 上确界 $[L_J,U_J]$、最大点区间、端点 ASN 与 P50/P90/P99；
- 相对固定基线的 ASN 节省率，以及相对 $\kappa=1$ 增加的最大抽检数；
- OC 曲线 $P_p(A)$，特别标出 $P_{p_0}(A)\ge0.95$、$P_{p_1}(A)\le0.10$；
- 推荐是否随 $p_1$ 或 $\kappa$ 改变。

若 $p_1-p_0$ 缩小时样本量陡增，这是质量分辨率的统计代价；若增大 $\kappa$ 只能微幅改善 ASN，则推荐 $\kappa=1$，不要为极小平均收益承担更长尾。论文不能只展示样本最少的 $p_1=0.20$ 或最大 $\kappa$。

### 4.7 复杂度、并行与缓存

设待评价候选数为 $G$，核验参数点数为 $P$。单候选的三角状态递推上界为 $O(N_{\max}^2)$，滚动概率向量为 $O(N_{\max})$；故

$$
T=O(GPN_{\max}^2),
\qquad
M_{\rm work}=O(N_{\max}).
$$

边界只依赖 $(p_0,p_1,h_A,h_R)$，应按哈希缓存；风险筛选阶段只跑 $p_0,p_1$ 两点，完整 OC 与分位数只对可行前沿候选计算。候选之间确定性并行，归并时按 `(J_upper,Nmax,endpoint_max_ASN,endpoint_mean_ASN,boundary_hash)` 固定排序，避免 worker 完成顺序改变推荐。

### 4.8 基准、性质测试与失败路径

| 检查 | 通过标准 | 失败处理 |
|---|---|---|
| 固定基线 oracle | 复现 4.2 表，风险差 $\le10^{-9}$ | 检查 CDF/SF 方向、整数截断数和尾概率实现 |
| Wald 初始化 | 允许不达标，但必须被精确 DP 识别 | 不得把近似阈值直接发布；进入风险校准 |
| 概率守恒 | 所有 OC 网格点 $P(A)+P(R)=1\pm10^{-10}$ | 定位边界重叠、漏分流或截尾遗漏 |
| 双风险 | $P_{p_0}(R)\le0.05$ 且 $P_{p_1}(A)\le0.10$ | `INFEASIBLE_RISK_PLAN` 或继续校准 |
| OC 单调性 | $P_p(A)$ 随 $p$ 非增，容差 $10^{-10}$ | 检查边界单调性和状态索引 |
| 二元终止 | 截尾后继续质量为 0 | `TERMINAL_MASS_REMAINS`，结果禁用 |
| 灰区目标认证 | $U_J-L_J\le\epsilon_{\rm ASN}$ | 未达标则 `SUCCESS_GRID_OBJECTIVE`，不得称族内最优 |
| 截尾倍率 | $N_{\max}\le\lfloor\kappa n_F\rfloor$ | 超限候选直接判不可行 |
| 非法早停 | 决策后不得读取后续样本 | 审计日志失败，整次运行无效 |
| 模型版本 | 配置含 `model_id=Q1-M6`、`algorithm_id=Q1-A2` | 旧 `summary.json` 标记 `STALE_MODEL_RESULT` |

最终 Q1 结果不得出现“未决率”“低未决方案”或旧 Pareto 二目标；若出现，说明代码仍在执行 Q1-M5/A1。

---
## 5. Q2-A1：16 策略精确枚举 + 吸收链奖励求解

### 5.1 输入与输出

| 类别 | 内容 |
|---|---|
| 输入 | 表 1 单个情形的 13 个字段，四位策略 $d=(x_1,x_2,x_r,y_r)$ |
| 输出 | 16 个策略的状态、利润、成本分账、事件次数、吸收裕度；最优策略集合 |
| 精确性口径 | 对已定义的 16 个固定策略全枚举；不扩张到历史自适应策略 |

### 5.2 状态图生成

算法只生成从初始订单可达的状态，不建立无用笛卡尔积。状态至少包含：

$$
s=(\ell,z_1,z_2,k_1,k_2,o_1,o_2),
$$

其中 $\ell$ 为流程位置，$z_i$ 为真实质量，$k_i$ 为已知信息，$o_i$ 为新购/拆回来源。状态采用不可变 tuple 并哈希；广度优先展开所有概率非零的转移，直到成功交付吸收态。

对每个瞬态状态记录：

- 到其他瞬态状态的概率；
- 直接进入成功吸收态的概率；
- 本步采购、检测、装配、拆解、调换和其他成本；
- 各类事件计数。

### 5.3 吸收性双重检查

1. **图检查**：在可达有向图上找强连通分量；若存在不含吸收泄漏的闭合可达分量，则标记 `NON_ABSORBING`；
2. **数值检查**：对通过图检查的瞬态矩阵 $P_d$ 计算最大模特征值或收敛上界，并记录吸收裕度
   $$
   \delta_d=1-\rho(P_d)>0.
   $$

图检查是有限状态链吸收性的主判据；$\varepsilon_\rho=10^{-10}$ 只是数值预警阈值，不是额外可行性条件。若 $0<\delta_d\le\varepsilon_\rho$，标记 `NEAR_NONABSORBING` 并用高精度复核，不能把有限但巨大期望的策略直接删掉；若数值谱半径与图检查冲突，先返回检查冲突并重算。不允许通过人为截断循环得到有限利润。

### 5.4 多右端奖励求解

不显式计算 $(I-P_d)^{-1}$。令

$$
A_d=I-P_d,
$$

把成本和所有事件计数向量拼成多右端矩阵

$$
B_d=[\mathbf c_d,\mathbf g_1,\ldots,\mathbf g_m].
$$

对 $A_d$ 只做一次稀疏 LU：

$$
A_dX_d=B_d.
$$

由初始状态对应行得到总成本和事件次数，利润为 $S-C(d)$。线性残差要求

$$
r_d=
\frac{\|A_dX_d-B_d\|_\infty}
{\|A_d\|_\infty\|X_d\|_\infty+\|B_d\|_\infty}
\le10^{-10}.
$$

失败时依次做行列缩放、迭代改进、稠密高精度对照；仍失败则返回 `ILL_CONDITIONED`，不参与最优比较。

### 5.5 策略比较与并列处理

完成 16 个策略后选择最大期望利润。若利润差满足

$$
|\Pi(d_i)-\Pi(d_j)|
\le10^{-8}\max\{1,|\Pi(d_i)|,|\Pi(d_j)|\},
$$

保留为并列最优集合；论文可再以“检测/拆解动作更少”作为透明的运营次级准则，但不能修改主目标值。

### 5.6 复杂度、预期范围与失败路径

若瞬态状态数为 $S_2$、非零元数为 $Z_2$，最坏稠密上界为 $O(16S_2^3)$，实际使用稀疏 LU，复杂度由填充量决定。Q2 规模很小，目标是正确而非追求元启发式速度。

预期检查：

- 一次装配成功率为 $(1-p_1)(1-p_2)(1-p_f)$，题给六种情形约在 0.512–0.8574；
- 期望采购、检测、装配、拆解、调换次数均非负；
- 期望利润不超过一次售价；
- 情形 6 拆解成本为 40，拆解价值应受到明显压制；
- 任一分账之和必须等于总成本，误差不超过 $10^{-8}$ 元。

---

## 6. Q3-A1：装配树局部核缓存 + 65536 策略精确搜索

### 6.1 输入与输出

| 类别 | 内容 |
|---|---|
| 输入 | 8 叶、3 半成品、1 成品的 AND 装配树与节点参数；16 位策略 |
| 输出 | 65536 个策略完整评价、全局最优固定策略集合、策略排名与分账 |
| 精确性口径 | 必须评价全部 $2^{16}$ 个固定策略；只在一般大树扩展时才允许近似回退 |

### 6.2 策略编码

固定使用位序：

```text
bits 0..7   : 零件1..8是否检测
bits 8..10  : 半成品1..3是否检测
bit 11      : 成品是否检测
bits 12..14 : 半成品1..3是否拆解
bit 15      : 成品是否拆解
```

编码表写入结果元数据，禁止在不同函数中重新定义位序。

### 6.3 后序局部状态核

对装配树做一次后序遍历。每个节点 $v$ 的局部状态核记录：

$$
K_v^{\pi_v}:
(\text{输入物料/质量/信息状态})
\longrightarrow
(\text{输出状态、概率、成本、事件计数}),
$$

其中 $\pi_v$ 是节点及其直接子树的局部决策签名。相同局部签名只计算一次。题给三个半成品子树分别只有有限种局部策略，缓存后再在根节点组合，避免为每个全局策略重复生成相同子树。

缓存只能合并“未来转移与奖励完全一致”的状态；仅概率接近或期望相同但信息不同的状态不得合并。每个核保存版本、子树哈希、参数哈希与状态字典。

### 6.4 全策略评价

1. 生成 0–65535 的全部策略整数并解码；
2. 查询/生成三个半成品子树核；
3. 在根节点组合成可达稀疏 $P_d$ 与多右端奖励矩阵；
4. 执行与 Q2 相同的 SCC+谱半径吸收检查；
5. 稀疏 LU 一次求全部成本与事件次数；
6. 写入分区 Parquet；
7. 所有 worker 完成后按策略整数排序归并，选择最大利润。

任务切片固定，例如每块 256 个策略；随机调度不能改变排序、并列规则或浮点归并次序。Ray/Dask 负责分布式执行，Numba 负责状态核与策略位运算。

### 6.5 正确性基准

| 基准 | 要求 |
|---|---|
| 树拓扑 | 半成品 1/2/3 分别接收 3/3/2 个零件，根接收 3 个半成品 |
| 无检测一次良率 | $0.9^{12}\approx0.2824$ |
| 物料守恒 | 每次父节点装配消耗每个直接子件各 1；拆解返回每个直接子件各 1 |
| 子树退化 | 只保留一个二零件子树时，与 Q2 对应策略结果一致 |
| 全策略覆盖 | 唯一策略 ID 数恰为 65536，无重复、无遗漏 |
| 精确最优口径 | 所有可吸收策略成功评价，或明确列出失败策略并完成高精度复核 |

### 6.6 复杂度与失败路径

设全策略数 $D=65536$，一次缓存后策略组合与求解成本为 $C_{\rm kernel}$：

$$
T=O(D\,C_{\rm kernel}),
$$

空间主要来自核缓存、单个策略稀疏矩阵和结果表；worker 不同时保留全部策略矩阵。

主线不因运行时间改用 GA/SA。若分布式任务失败，先重试确定性分片；仍失败则扩容。只有当题目推广到远大于当前 16 位策略时，才进入分支定界或近似算法，并另行降低最优性称谓。

---

## 7. Q4-A1：全策略后验 Monte Carlo

### 7.1 后验接口

每个参数 $p_j$ 在先验 $\operatorname{Beta}(a_j,b_j)$ 下有

$$
p_j\mid N_j,K_j
\sim
\operatorname{Beta}(a_j+K_j,b_j+N_j-K_j).
$$

主线运行两组先验：

- Uniform：$(a_j,b_j)=(1,1)$；
- Jeffreys：$(a_j,b_j)=(1/2,1/2)$。

二者都是分析口径，不得写成题面事实。若题面仍无实际 $(N_j,K_j)$，只生成参数化接口和明确标注的模拟示例。

### 7.2 全策略共同随机数

为最大化结果质量，不使用只保留 nominal top-$K$ 的启发式筛选。对同一个后验样本 $\mathbf p^{(b)}$，Q2 的 16 个或 Q3 的 65536 个策略全部使用完全相同的参数样本，并由 Q2-A1/Q3-A1 精确评价：

$$
Y_b(d)=\Pi(d;\mathbf p^{(b)}).
$$

共同随机数通常会降低策略利润差

$$
\Delta_b(d_1,d_2)=Y_b(d_1)-Y_b(d_2)
$$

的 Monte Carlo 方差；是否确有降幅必须用配对样本方差与独立抽样基准实测，不能预先保证“显著降低”。每个样本内先找精确固定策略最优；若并列，最优概率在并列策略间等分，同时保留“任一并列最优”的计数。

### 7.3 分批、自适应精度停止

默认配置：

```text
seed = 2024
confirm_seed = 12024
B0 = 2048
batch_size = 2048
Bmax_Q2 = 262144
Bmax_Q3 = 65536
epsilon_profit = max(0.01, 0.001 * sale_price)
epsilon_prob = 0.005
stable_checkpoints = 3
min_batches = 8
```

每批更新后验期望利润、标准误、最优概率和 regret。配对利润差半宽使用批次均值的双侧 95% Student-$t$ 区间。只有同时满足以下条件才结束探索批次：

1. 当前前两名策略的配对利润差置信半宽 $\le\epsilon_{\rm profit}$；
2. 前两名顺序连续三个 checkpoint 不变；
3. 领先策略最优概率的标准误 $\le\epsilon_{\rm prob}$；
4. 已完成不少于 8 个批次，避免用极少批次估计 Student-$t$ 半宽；
5. Uniform 与 Jeffreys 两条主运行均达到各自标准，或明确报告先验导致策略改变。

探索停止后再使用预先固定、与探索样本独立的确认批次（Q2 默认 32768 个、Q3 默认 16384 个样本）复核排名与误差；这样避免把同一批数据同时用于“决定何时停止”和最终确认。达到 $B_{\max}$ 仍不满足，或独立确认批次推翻领先顺序时，返回 `MC_NOT_CONVERGED`，输出近优策略集合和现有误差，不强行给唯一最优。

### 7.4 输出

对每个策略至少报告：

$$
\widehat{\mathbb E}[\Pi_d],\quad
\operatorname{SE}_{MC}(\Pi_d),\quad
\Pr(d\text{ 最优}\mid\mathcal D),
$$

$$
\Pr(\Pi_d<0\mid\mathcal D),\quad
\mathbb E[R_d\mid\mathcal D],\quad
Q_{0.05}(\Pi_d),Q_{0.50}(\Pi_d),Q_{0.95}(\Pi_d).
$$

后验分布区间是 Bayesian 可信区间；MC 标准误只描述数值积分精度，两者不能混称。

### 7.5 复杂度与工具

若策略数为 $D$、后验样本数为 $B$、单次策略评价成本为 $C$：

$$
T=O(BDC).
$$

Q4-Q3 是全题最大计算任务，因此使用 Ray/Dask 集群、Numba 状态核、批量 Parquet 和共同随机数。工具充足并不取消误差停止；它只允许扩大 $B$，不能把一次随机结果当结论。

评价器必须实现 `evaluate_batch(policy_block, parameter_block)`：固定状态拓扑和局部核先编译，JAX `jit/vmap` 在 GPU/TPU 上批量计算，禁止在 Python 层对每个“策略×样本”单独建图和分解矩阵。达到 Q3 的探索上限时，每个先验最多涉及 $65536^2\approx4.29\times10^9$ 个策略—样本组合，独立确认批次另有约 $1.07\times10^9$ 个组合；在线阶段流式累计均值、方差和最优计数，不把完整矩阵装入内存。若论文需要精确经验分位数，则把 `float64` 利润块写入 Zarr 共享存储，最终按策略块复算；若只保存竞争策略原始样本，其他策略的分位数必须明确标注为流式近似。

---

## 8. Q4-A2：联合 CS + 单调性认证 + 稳健内层搜索

### 8.1 联合不确定集

设 Q2 有 $J=3$ 个质量参数，Q3 有 $J=12$ 个。主线采用 90% 联合置信集合，95% 作保守敏感性：

$$
\alpha_{\rm total}\in\{0.10,0.05\},
\qquad
\alpha_j=\frac{\alpha_{\rm total}}{J}.
$$

对每个参数用 Howard 同族的 Bernoulli CS 构集（这是 Q4-A2 的独立构集部件，不再引用已退场的 Q1-A1 编号），得到

$$
\mathcal C_j=[L_j,U_j],
\qquad
\mathcal U=\prod_{j=1}^J[L_j,U_j].
$$

Bonferroni 较保守，但联合覆盖透明；不根据已看到的利润结果重新分配 $\alpha_j$。

### 8.2 先做单调性认证

对每个固定策略 $d$，先检查是否能证明

$$
\frac{\partial\Pi(d;\mathbf p)}{\partial p_j}\le0,
\qquad \forall\mathbf p\in\mathcal U, j=1,\ldots,J.
$$

认证分三层：

1. **结构证明**：所有成本非负、缺陷不会产生收入、固定策略下增加缺陷只能增加重试/报废/调换或保持不变；用共同均匀随机数耦合低缺陷率与高缺陷率系统；
2. **自动微分检查**：在角点、中心、Sobol 网格上检查 Jacobian 符号；此层只用于找反例，不能单独构成证明；
3. **区间导数认证**：用区间算术证明整个盒内导数上界不大于 0。

若三层完成且无例外，则

$$
\Pi_{\min}(d)=\Pi(d;U_1,\ldots,U_J)
$$

为严格最坏点，Q4-A2 退化为单端点评价。若证明只覆盖部分参数，只固定这些参数到上端点，其余维度继续全局搜索。

### 8.3 无单调性证书时的搜索

对未认证的 $r$ 个维度执行：

1. 枚举 $2^r$ 个角点；
2. 用 `SHGO` 搜索连续内部极小值；
3. 用不少于 10 个固定种子的 differential evolution 独立互证；
4. 对最终竞争策略使用区间分支定界，直到
   $$
   U_d^{\rm inner}-L_d^{\rm inner}\le\epsilon_{\rm robust},
   $$
   默认 $\epsilon_{\rm robust}=\max(0.01,0.001S)$；
5. 外层仍覆盖全部 16/65536 个固定策略；允许用严格上下界淘汰“不可能击败当前稳健 incumbent”的策略，但不得用 nominal top-$K$ 启发式删去未证明劣势的策略。

默认搜索预算冻结为：

```text
SHGO: sampling_method="sobol", n=8192, iters=5
DE: seeds=2024..2033, popsize=max(20, 10*r), maxiter=3000
    tol=1e-9, atol=1e-10, polish=true
Interval BnB: max_boxes=10_000_000, max_walltime=6 h / competitive policy
Outer parallelism: at most 512 concurrent policy tasks
```

这里的“competitive policy”必须由严格上下界筛出，而不是按 nominal 排名前若干名定义。任一预算先耗尽而 gap 未达标，就保留当前上下界并降为 `ROBUST_UNCERTIFIED`；增加硬件只允许扩展预算，不改变状态判定。

严格区分：

- `ROBUST_CERTIFIED`：内层有有效上下界且 gap 达标；
- `ROBUST_NUMERICAL`：SHGO/DE/角点一致但没有区间证书；
- `ROBUST_UNCERTIFIED`：搜索结果分歧或达到预算仍未收敛。

只有第一种能写“矩形不确定集内的认证稳健最优”。第二种只能写“数值搜索所得稳健方案”。

### 8.4 预期关系

若 posterior mean 或 nominal 参数位于 $\mathcal U$，则同一策略应满足

$$
\Pi_{\min}(d)\le\Pi(d;\mathbf p_{\rm nominal}).
$$

联合样本量增加时，$[L_j,U_j]$ 原则上收窄，最坏利润不应系统性下降；若下降，优先检查停止规则、批次漂移或参数条件定义，而不是直接解释为经济现象。

---

## 9. 五个算法的通用契约

| 项目 | Q1-A2 | Q2-A1 | Q3-A1 | Q4-A1 | Q4-A2 |
|---|---|---|---|---|---|
| 输入 | Bernoulli 流、$p_0,p_1,\alpha,\beta$ | 表1情形、4位策略 | 装配树、16位策略 | $(N_j,K_j)$、先验、生产评价器 | simultaneous CS、评价器、搜索容差 |
| 输出 | 固定基线、二元边界、双风险、OC、灰区最坏 ASN/分位数 | 16策略利润与分账 | 65536策略利润与分账 | 后验利润、最优概率、遗憾 | 最坏利润、最坏点、认证状态 |
| 初始化 | 精确二项 $(n_F,c_F)$ + Wald LLR 边界 | 初始待交付订单 | 后序树核缓存 | seed=2024，共同随机数 | 角点、中心和单调性证书任务 |
| 参数 | $p_1,\kappa,N_{\max},c_N,h_A,h_R$ | 数值容差 | chunk、worker、容差 | 先验、B、误差容差 | $\alpha_{\rm total}$、全局搜索与区间 gap |
| 停止 | 预声明断点族搜索完成并通过精确风险门禁 | 16策略完成 | 65536策略完成 | MC 精度达标或 $B_{\max}$ | 单调性证明或内层 gap 达标/预算耗尽 |
| 计算预算 | 二项基线 + 断点搜索 + 状态 DP | 小规模稀疏 LU | 分布式全枚举 | 分布式全策略×批次 | 全外层策略×内层认证 |
| 随机性 | 无 | 无 | 无 | 固定 seed、批次、共同随机数 | DE 有固定种子；SHGO/区间法确定性 |
| 状态 | exact_family/grid_objective/local_calibration/infeasible/stale | exact/near_nonabsorbing/non_absorbing/ill_conditioned | exact/worker_failed/incomplete | converged/not_converged | certified/numerical/uncertified |
| 产物 | 固定基线表、边界表、OC/ASN 表、灰区上确界证书、风险校准日志 | 策略表、状态图、残差 | 分片 Parquet、核缓存、总排名 | posterior draws 摘要、误差轨迹 | 角点表、搜索轨迹、区间上下界 |
| 失败路径 | 高精度复算；风险不达标则回退精确固定方案，绝不保留未决 | 修复状态图，不截断伪解 | 重跑分片/扩容，不先上 GA | 扩大 B 或报告近优集合 | 扩大认证预算或降低结论称谓 |

### 9.1 求解器专项检查

当前主线没有把 Q1–Q4 写成 LP/MIP 并交给数学规划求解器，因此 `incumbent/bound/MIP gap/IIS` **不适用**。Q3 的全枚举最优性来自策略全集覆盖；Q4 内层的 `gap` 仅指区间全局搜索上下界，不能称作 MIP gap。

### 9.2 日志与复现

每次运行必须写入：

- Git/代码版本与配置文件哈希；
- Python、依赖库、操作系统与硬件；
- 输入文件哈希；
- 策略位序、状态 schema 版本；
- 随机种子、批次大小、worker 数；
- 容差、停止原因、失败状态；
- 运行时间、峰值内存、中间产物路径。

---

## 10. 复杂度总表与主要瓶颈

| 算法 | 时间复杂度主项 | 空间主项 | 瓶颈 | 主要加速 |
|---|---|---|---|---|
| Q1-A2 | $O(GPN_{\max}^2)$ | 工作 $O(N_{\max})$；保留边界表 $O(N_{\max})$ | LLR 断点候选与精确端点 DP | 风险先剪枝、断点索引、候选并行、边界缓存 |
| Q2-A1 | $O(16\,C_{LU}(S_2))$ | 单策略稀疏矩阵 | 状态定义正确性 | 一次 LU 多右端 |
| Q3-A1 | $O(65536\,C_{kernel})$ | 局部核+单策略矩阵 | 全策略状态组合与求解 | 子树核缓存、Numba、Ray |
| Q4-A1 | $O(BD C_{eval})$ | 批次利润矩阵 | Q3 全策略×后验样本 | 共同随机数、批次、集群 |
| Q4-A2 | $O(D C_{inner})$ | 搜索队列与区间盒 | 12维连续最坏搜索 | 单调性认证、角点、区间并行 |

效果优先不意味着无预算概念：若 Q4-Q3 认证计算超出比赛时间，允许报告 `ROBUST_NUMERICAL`，但不允许把未完成的认证包装成严格全局最优。

---

## 11. 适应性、扩展性与局限性

### 11.1 规模与精度适应性

| 算法 | 本题规模是否适合 | 扩展到更大规模时 | 精度口径 |
|---|:---:|---|---|
| Q1-A2 | ✅ 主方案最大 $n_F=2096$；$\kappa=2$ 敏感性上限 4192，有限状态 DP 可完成 | 更窄 $p_1-p_0$ 时用活动前沿、风险剪枝、灰区区间界和候选并行；仍不以 MC 替代精确停止概率 | 二项尾概率、路径概率、灰区 ASN 上界与风险裕度分别核验 |
| Q2-A1 | ✅ 仅 16 个固定策略 | 若允许依历史动态改策略，策略空间改变，必须另建 MDP/POMDP，不能沿用“16 策略全局最优”称谓 | 全枚举误差为零，保留线性代数误差 |
| Q3-A1 | ✅ 16 位、65536 个固定策略 | 决策位翻倍会增长到 $2^{32}$；届时需严格分支定界或树 DP，并重新说明最优性证据 | 全策略覆盖才是固定策略域精确最优 |
| Q4-A1 | ✅ 可用集群批量计算 | 参数数或策略数增加时优先向量化、局部核复用和分布式批次；样本量仍由误差而非算力任意决定 | 后验不确定性与 MC 数值误差分开 |
| Q4-A2 | ⚠️ 12 维盒约束的区间认证可能昂贵 | 先证明单调维度、再降维；严格上下界只淘汰已证明劣势的外层策略 | 有区间 gap 才能使用 certified 称谓 |

算法接口对 Q4 的扩展是显式的：Q2/Q3 的生产评价器只接收参数向量与策略，不读取全局变量，因此既可被后验样本批量调用，也可被稳健内层搜索调用。装配树增加层级时只扩展 `Node/Edge` schema 和局部核，不改变统一奖励求解器。

### 11.2 局限性登记

| # | 局限性 | 可能影响 | 阶段4必须设计的缓解/检验 |
|---|---|---|---|
| L1 | 算法精确性以阶段2的独立缺陷率、恒定参数和状态定义正确为条件 | 状态遗漏会产生“数值精确但语义错误”的结果 | 手算微型链、事件追踪、退化到一次流程与 Q2 子树对照 |
| L2 | Q1 的 $p_1$ 不是题面数据；阈值过近会使样本量陡增，过远又会放松使用方保护 | 推荐可能被工程偏好主导 | 固定 $p_0,\alpha,\beta$，对 $p_1=0.12/0.13/0.15/0.20$ 全流程敏感性并公开 OC/ASN |
| L3 | $\rho(P_d)$ 接近 1 时，期望成本和事件次数对浮点误差敏感 | 利润排名不稳定 | SCC 图检查、条件数/残差、缩放、迭代改进和高精度复算 |
| L4 | Q3 的“精确”只针对 16 位固定策略，不覆盖历史自适应策略 | 结论范围被误写为所有可能政策最优 | 在论文模型边界和策略编码表中重复声明 |
| L5 | Q4 若没有真实 $(N_j,K_j)$，只能做参数化或明确模拟 | 无法形成真实后验决策 | 数据到位前禁止把模拟样本写成题给数据 |
| L6 | Uniform/Jeffreys 先验可能在小样本下改变排序 | Bayesian 最优策略对先验敏感 | 双先验、先验强度敏感性与独立确认批次 |
| L7 | Bonferroni 矩形集合保守，区间分支定界受维数灾难影响 | 稳健利润偏低或认证超时 | 报告 90%/95% 两档、先做单调性证书、保留 numerical/uncertified 状态 |
| L8 | 共同随机数不保证对所有策略对都降方差 | 配对误差可能无改善 | 与独立抽样小基准比较，按实测方差决定是否保留 |
| L9 | Q4 的 ConfSeq 官方库与独立反演在极端 $(t,k)$ 可能下溢或端点分歧 | 联合置信集错误 | log 域、`mpmath` 复核、固定点二项基准与全状态交叉测试 |

---

## 12. 预期输出范围与运行前 sanity check

| 输出 | 预期/约束 | 依据 |
|---|---|---|
| Q1 固定双风险基线 | $p_1=0.13$ 时应复现 $(n_F,c_F)=(968,112)$，$\alpha_F\approx0.04865,\beta_F\approx0.09976$ | 精确二项 OC 门禁 |
| Q1 序贯效率与终止 | 主方案 $\kappa=1$ 时 $J\le n_F$；所有 $p$ 上 $P(A)+P(R)=1$；$p_1$ 越接近 0.10，灰区最坏 ASN 越大 | 固定方案属于可行候选；Q1-M6/Q1-A2 |
| Q2 一次成功率 | 约 0.512–0.8574 | 表1次品率乘法基准 |
| Q2/Q3 利润 | 不超过一次售价；允许因高成本为负 | 收入只计一次、成本非负 |
| Q3 无检测一次良率 | $0.9^{12}\approx0.2824$ | 8叶+3半成品+1成品条件缺陷 |
| Q2/Q3 事件次数 | 全部非负；分账和等于总成本 | Markov reward 定义 |
| Q4 后验概率 | 各策略最优概率之和为 1；遗憾非负 | 概率与 regret 定义 |
| Q4 区间 | 样本量增加时总体趋于收窄 | Beta 后验与 CS |
| Q4 稳健利润 | nominal 位于集合时，最坏利润不高于 nominal 利润 | max–min 定义 |

[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/yum1981/yum1981.md|Yum 与 McDowell（1981）]]算例中的 80.0993/件等数值只用于检验代码数量级，不是 B 题答案；不同成本结构不能直接搬用。

---

## 13. 状态码与失败关闭

| 状态码 | 含义 | 是否可进入论文主结论 |
|---|---|:---:|
| `SUCCESS_EXACT` | 确定性全搜索与数值检查通过 | ✅ |
| `SUCCESS_MC_TOL` | MC 达到预设误差 | ✅，附误差 |
| `SUCCESS_EXACT_FAMILY` | Q1 预声明截尾 SPRT/边界族完整搜索且精确风险核验通过 | ✅，结论限定为族内最优 |
| `SUCCESS_GRID_OBJECTIVE` | 边界族搜索完成，但灰区 ASN 上确界只按离散网格近似 | ⚠️，只能称网格目标下最优 |
| `SUCCESS_LOCAL_CALIBRATION` | Q1 仅在 Wald 邻域完成可行校准 | ⚠️，只能称可行改进方案 |
| `INFEASIBLE_RISK_PLAN` | 给定 $p_1,N_{\max}$ 下没有通过双风险门禁的候选 | ❌，扩大截尾/搜索或回退固定基线 |
| `RISK_NOT_EXACTLY_VERIFIED` | 只用了 Wald/正态近似，未做精确二项路径核验 | ❌ |
| `TERMINAL_MASS_REMAINS` | 截尾后仍有继续概率质量 | ❌，违反二元终止 |
| `STALE_MODEL_RESULT` | 结果来自旧 Q1-M5/A1 配置或旧 `summary.json` | ❌ |
| `NEAR_NONABSORBING` | 图检查通过但吸收裕度不超过 $10^{-10}$ | ⚠️，高精度复核通过后方可进入 |
| `NON_ABSORBING` | 策略存在非终止闭类 | ❌，标不可行 |
| `ILL_CONDITIONED` | 奖励方程无法稳定求解 | ❌，高精度复核后再定 |
| `INCOMPLETE_POLICY_SET` | Q3 未覆盖全部 65536 策略 | ❌，不得称精确最优 |
| `MC_NOT_CONVERGED` | 达 $B_{\max}$ 仍未满足 MC 容差 | ⚠️，只报告近优集合 |
| `ROBUST_NUMERICAL` | 多算法一致但无严格内层证书 | ⚠️，降低称谓 |
| `ROBUST_UNCERTIFIED` | 最坏搜索分歧或未收敛 | ❌，不得给稳健最优 |
| `INVALID_DATA` | 概率、成本、树或 $(N,K)$ 非法 | ❌ |
| `INVALID_CONDITIONING` | 内部节点样本不对应条件次品率 | ❌ |

---

## 14. 科学性与论文依据审查

| 算法 | 公认方法 | 本地论文依据 | 本题扩展 |
|---|---|---|---|
| Q1-A2 | 精确二项 OC、Wald SPRT、有限状态路径 DP、双风险校准 | [[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/Zhang_2023_Imperfect_Inspection_Acceptance_Sampling/Zhang_2023_Imperfect_Inspection_Acceptance_Sampling.md|张斌 2023]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/Duan_Zhang_2026_Electronic_Assembly_Quality_Control/Duan_Zhang_2026_Electronic_Assembly_Quality_Control.md|Duan 2026]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/Wald_1945_Sequential_Hypothesis_Tests/Wald_1945_Sequential_Hypothesis_Tests.md|Wald 1945]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/Optimal Stopping for Interval Estimation in Bernoulli Trials/Optimal Stopping for Interval Estimation in Bernoulli Trials.md|Yaacoub 2019]] | $p_0,p_1,\alpha,\beta$ 双风险接口；强制截尾；精确端点校准；灰区最坏 ASN 与截尾倍率选择 |
| Q2-A1 | 吸收链基本矩阵、策略评价 | [[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/britney1972/britney1972.md|Britney 1972]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/kao1995/kao1995.md|Kao 1995]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/Chevalier_Wein_1997_Circuit_Board_Inspection/Chevalier_Wein_1997_Circuit_Board_Inspection.md|Chevalier 1997]] | 16 策略、真实质量回流、双吸收检查 |
| Q3-A1 | AND 产品树、条件拆解 DP、吸收链 | [[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/meacham1999/meacham1999.md|Meacham 1999]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/teunter2006/teunter2006.md|Teunter 2006]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/britney1972/britney1972.md|Britney 1972]] | 局部核缓存和 65536 全策略组合 |
| Q4-A1 | Beta 共轭、后验 MC、共同随机数 | [[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/FallahNezhad_2012_Bayesian_Acceptance_Sampling/FallahNezhad_2012_Bayesian_Acceptance_Sampling.md|Fallah 2012]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/Zhang_2023_Imperfect_Inspection_Acceptance_Sampling/Zhang_2023_Imperfect_Inspection_Acceptance_Sampling.md|张斌 2023]] | 全策略共同样本与误差停止 |
| Q4-A2 | simultaneous CS、矩形集 min–max、全局搜索 | [[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/Time-uniform, nonparametric, nonasymptotic confidence sequences/Time-uniform, nonparametric, nonasymptotic confidence sequences.md|Howard 2021]]；[[国赛练习题/CUMCM2024Problems/B题/参考文献/MD/Duan_Zhang_2026_Electronic_Assembly_Quality_Control/Duan_Zhang_2026_Electronic_Assembly_Quality_Control.md|Duan 2026]] | 单调性先认证、不能照搬端点结论、区间证书 |

本报告没有新增未建立证据链的 POMDP、CVaR 或元启发式主模型。工具是实现载体，不作为论文理论依据的替代品。

---

## 15. 可复用 Cookbook、模板与需新实现组件

### 15.1 直接参考但必须按本题改写

| 组件 | Solver Skills 参考 | 可复用内容 | 必须改写/补充 |
|---|---|---|---|
| Q1 精确路径 DP | `.agents/skills/math-modeling-solver/references/cookbook-optimization.md` 第5节 | 状态、转移、边界和自底向上递推框架 | 改为 Bernoulli $(t,k)$ 概率质量分流与双停止边界 |
| Q2/Q3 Markov 验证 | `.agents/skills/math-modeling-solver/references/code-templates/python/ml/markov_chain_template.py` | 转移矩阵构造、行和检查的代码骨架 | 新增可达状态生成、吸收性检查、稀疏奖励方程和成本分账；模板不能原样充当吸收奖励求解器 |
| Q4 后验模拟 | `.agents/skills/math-modeling-solver/references/cookbook-statistical.md` 第4–5节；`code-templates/python/optimization/monte_carlo_template.py` | RNG、分批统计、Beta–Binomial 共轭与基础收敛检查 | 改为全策略共同参数样本、配对误差、独立确认批次和后验遗憾 |
| 通用失败关闭 | `.agents/skills/math-modeling-solver/references/algorithm-and-solver-engineering.md` | 输入输出契约、停止条件、日志、数值异常与复现清单 | 映射到本报告状态码，不套用不适用的 MIP gap/IIS |

### 15.2 必须根据论文与官方库新实现

| 组件 | 依据 | 实现要求 |
|---|---|---|
| 精确二项方案搜索器 | 张斌 2023 + Duan 2026 | 递增搜索 $(n,c)$、稳定 CDF/SF、四档 $p_1$ oracle 与风险裕度 |
| 截尾 SPRT 风险校准器 | Wald 1945 + Yaacoub 2019 | LLR 整数边界、强制终端截断、可达断点索引、精确路径 DP 与族内最优日志 |
| 灰区 ASN 上确界认证器 | 有限 Bernoulli 路径多项式 + 区间/Bernstein 包络 | 输出最大点区间、$[L_J,U_J]$、容差与细分证书；不得以稀疏网格冒充连续上确界 |
| Q4 Bernoulli CS 独立反演器 | Howard 2021 + ConfSeq 官方接口 | log 域 mixture、端点根求解、全 $(t,k)$ 缓存、与官方结果逐点交叉核验；不供 Q1-M6 主线调用 |
| 装配树局部状态核 | Meacham 1999、Teunter 2006 与阶段2状态定义 | 后序遍历、局部策略签名、物料/质量/信息/来源守恒、哈希缓存 |
| 吸收 Markov 多奖励求解器 | Britney 1972、Kao 1995 | SCC、谱半径、稀疏 LU 多右端、残差与高精度回退 |
| 单调性与区间认证桥接 | Q4-M3 及区间工具官方接口 | JAX 只找反例；Julia/IBEX 给盒内导数或目标上下界，并把证书与策略 ID 一起保存 |

因此阶段6的实现策略是“复用骨架、重写数学内核”，而不是为了模板匹配而替换已确认模型。

---

## 16. 阶段3推荐结论

> [!summary] Q1 重构后的锁定算法组合
> 1. **Q1**：最小固定样本精确二项基线；Wald 截尾 SPRT 初始化；可达 LLR 断点风险校准；有限状态 DP 精确核验；灰区 ASN 连续上确界认证；对 $p_1=0.12/0.13/0.15/0.20$ 与 $\kappa=1/1.25/1.5/2$ 做 OC/灰区 ASN 双重敏感性；
> 2. **Q2**：16 策略全枚举；可达状态图；SCC+谱半径吸收检查；稀疏 LU 多右端求解；
> 3. **Q3**：后序局部核缓存；65536 策略分布式全枚举；不使用 GA/SA 替代；
> 4. **Q4 Bayesian**：Uniform/Jeffreys 双先验；全策略共同随机数后验 MC；按配对利润差误差停止；
> 5. **Q4 稳健**：90% 联合集合为主、95% 敏感性；先认证单调性，失败后角点+SHGO+DE+区间分支定界；
> 6. **工具策略**：缺工具时安装或建立独立环境，不自动降低算法；若比赛时间不足，只降低结论称谓，不伪造认证。

本次 Q1 模型—算法同步已完成；现有 Q1-M5/A1 代码与结果随之失效。下一步应按本节契约重写 Q1 代码，再进入**阶段4：模型检验方案设计**，把双风险、OC、灰区 ASN、$p_1$/$\kappa$ 敏感性和失败状态转成可执行检验清单。
