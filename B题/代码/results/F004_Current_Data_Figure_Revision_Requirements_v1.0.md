# F004_Current_Data_Figure_Revision_Requirements_v1.0

**文档用途**：当前仓库已生成数据图的修改任务单，仅列需要队友进一步修改或核验的图片。  
**审计基线**：`6f94390a2e67c60e548a36a0c1720c9ec31b0156`（`图表转成pdf`）  
**审计范围**：Q1–Q4 已上传 PDF、对应 MATLAB 绘图脚本及其直接读取的 CSV/JSON 结果文件。  
**状态**：`READY_FOR_TEAMMATE`  
**性质**：`NON-AUTHORITATIVE WORKING VIEW`。本文件用于修图协作，不替代 Result Registry、Project State 或正式论文图表清单。

> 说明：本次审计重点是“图所读取的数据是否正确、图想表达的 claim 是否是论文所需、绘图逻辑是否会造成误导”。最终 PDF 的字体、裁切、标签重叠和灰度打印仍需本地做 render check。

---

# 1. 需要修改的图片总表

| 图编号 | 当前图片/脚本 | 核心结论（一句话） | 当前问题 | 必须修改内容 | 直接数据来源 | 修改后验收标准 | 优先级 | 预期位置 |
|---|---|---|---|---|---|---|---|---|
| **Q1-F2** | `q1_2主情景的OC与ASN.pdf` / `Fig_Q1_2_oc_asn.m` | 主情景同时满足生产方与使用方风险约束，且最坏平均抽样量出现在无差异区内部 | 图内总标题带内部状态码 `SUCCESS_LOCAL_CALIBRATION`；0.95 与 0.10 水平风险线没有明确说明各自代表什么 | ① 删除图题中的内部状态码；② 将 0.95 风险线标为 `1-α=0.95`，0.10 风险线标为 `β=0.10`；③ 保留 `p0=0.10`、`p1=0.13`、端点风险值和 ASN 最坏点 | `results/q1/summary.json`；`results/q1/operating_characteristics.csv` | 图中不出现内部程序状态；读者无需看正文即可判断两条水平线含义；端点风险与 `p_worst=0.112375, J_ASN≈923.103` 保持不变 | **P0** | **正文核心** |
| **Q3-F2** | `q3_2关键参数敏感性与策略切换.pdf` / `Fig_Q3_2_sensitivity_switch.m` | 名义最优策略对大多数缺陷率与成本扰动稳定，仅当最终成品缺陷率取 20% 时切换到 65535 | 当前脚本把 5%/20% 描述为 `official Table 1-2 observed defect-rate range`，容易被理解成“Q3 官方参数范围为 5%–20%”；±25% 成本扰动需要更明显标为人为压力测试 | ① 图注/脚本注释改为“采用题面各参数表中出现的 5% 与 20% 缺陷率水平构造离散敏感性情景”；② 明确“±25% 为人为成本压力测试，不是题面给定范围”；③ 保留 `★` 表示相对名义策略发生切换；④ 明确突出 `p_F=20% → 65535` | `results/q3/sensitivity.csv` | 不再声称 Q3 的官方连续范围是 5%–20%；左、右面板证据性质区分清楚；唯一策略切换点与 CSV 一致 | **P0** | **正文核心** |
| **Q4-F2** | `q4_2演示抽样情形下问题三各质量参数的边际净信息价值.pdf` / `Fig_Q4_2_q3_net_kg.m` | 在当前演示证据下，Q3 的所有追加检测动作净信息价值均为负，因此一步 KG 建议停止追加抽样 | 12 个动作共用同一线性横轴，而数值约从 `-1` 到 `-106`，导致 8 个零件层动作全部挤在 0 附近，主要比较信息几乎不可读；先验标题仍为英文 | **必须重画版式**：① 每个先验至少分为“零件1–8”与“半成品1–3+最终成品”两个尺度面板，或采用明确断轴；② 保留每个动作的 `net_KG` 与 95% 区间；③ `uniform prior` 改“均匀先验”，`jeffreys prior` 改“Jeffreys先验”；④ 保留 `KG=0` 参考线；⑤ 建议在点旁直接标数值；⑥ 图注明确这是 `myopic-KG` / 一步 KG，不是全局序贯最优性证明 | `results/q4/q3_kg_action_values.csv` | 零件层约 `-1~-2` 的差异能够肉眼辨认；半成品约 `-35~-38`、最终成品约 `-106` 仍清晰；所有区间上界均低于0的结论一眼可读；不得用统一大尺度压扁零件层 | **P0 / 最高** | **正文核心** |
| **Q4-F3** | `q4_3演示抽样情景下Bayesian与数值稳健策略的最坏利润比较.pdf` / `Fig_Q4_3_robust_audit.m` | 期望利润导向的终止策略与最坏情形防御策略并不总一致，Q2 情形5和 Q3 出现明显策略切换 | 图题/图例只写“Bayesian终止策略”，没有指出 robust audit 的 baseline 实际绑定当前 `main_prior=uniform`；结果 CSV 本身也缺少 `prior` 字段 | ① 图题/图例改为“Bayesian终止策略（Uniform主先验）”或“均匀主先验终止策略”；② **建议**在 `robust_audit.csv` 增加 `prior=uniform` 字段，提高结果文件自描述性；③ 保留 `ROBUST_NUMERICAL` 限定；④ 图注继续明确“不表示严格认证的全局稳健最优”；⑤ 保留策略切换位串标记 | `results/q4/robust_audit.csv`；`q4/config.json`（`main_prior="uniform"`）；`q4/run_q4.py` | 读者能明确知道 baseline 只对应 Uniform 主先验；Q2 情形5 `0110→1101`、Q3 `63487→65535` 等切换与原始 CSV 一致；不得写成 `ROBUST_CERTIFIED` 或“全局稳健最优” | **P0** | **正文强候选** |

---

# 2. 条件性修正 / 必须做最终渲染核验

| 图编号 | 当前图片/脚本 | 需要核验的问题 | 若发现问题如何修改 | 数据来源 | 优先级 |
|---|---|---|---|---|---|
| **Q3-F1** | `q3_1Top5可行固定策略.pdf` / `Fig_Q3_1_top5_profit_loss.m` | MATLAB `readtable` 读取 16 位 `strategy_bits` 时，最终 PDF 是否仍显示完整位串，而不是科学计数法或精度丢失 | 若出现 `1.111...e15` 等形式，强制将 `strategy_bits` 按字符串导入/转换，标签必须完整显示如 `1111111111101111` | `results/q3/top10_policies.csv` | **P1 / render check** |

Q3-F1 的数据和图意本身已经通过，本项不是要求重做图，只是正式冻结前必须确认标签渲染。

---

# 3. 各图必须保留的关键数值与 claim

## Q1-F2

必须与当前结果保持一致：

- 主情景：`p0=0.10, p1=0.13, κ=1`
- 生产方风险：`P_{p0}(R) ≈ 0.048913 < 0.05`
- 使用方风险：`P_{p1}(A) ≈ 0.099597 < 0.10`
- 最坏 ASN 网格点：`p ≈ 0.112375`
- `J_ASN ≈ 923.103`

正文 claim：

> 在当前主设计情景及局部校准所得截尾 SPRT 方案下，两端风险约束均满足；无差异区内部存在更难判定的质量水平，其平均抽样量高于两个端点。

不得把 `SUCCESS_LOCAL_CALIBRATION` 当成图中学术结论。

---

## Q3-F2

当前数据中：

- 大多数 5%/20% 缺陷率离散情景下仍为策略 `63487`
- `p_F=0.20` 时切换为 `65535`
- ±25% 的六类成本压力测试均未改变名义最优策略

正文 claim：

> 当前固定策略域内的最优决策对多数单因素扰动保持稳定，而最终成品缺陷率是较关键的结构敏感因素。

注意：

- 5%/20% 是**离散情景水平**
- ±25% 是**人为压力测试**
- 不得声称求得连续参数临界点

---

## Q4-F2

当前两种先验下均有：

- 零件层 Net KG 约为 `-1 ~ -2 元/件`
- 半成品层约为 `-35 ~ -38 元/件`
- 最终成品约为 `-106 元/件`
- 当前保存的 95% 区间上界均 `< 0`
- 当前状态：`STOP_NO_POSITIVE_NET_VOI`
- optimality label：`myopic-KG`

正文 claim：

> 在当前演示抽样证据与成本设定下，所有候选追加检测动作的一步净信息价值均为负，因此当前一步 KG 规则选择停止追加抽样。

不得写：

> “证明以后任何序贯抽样都没有价值。”

---

## Q4-F3

当前 robust audit 的 baseline 来自：

```text
main_prior = uniform
```

关键切换包括：

- Q2 情形5：`0110 → 1101`
  - 90%：最坏利润约 `-2.640 → 1.454`
  - 95%：约 `-5.038 → 0.245`
- Q3：`63487 → 65535`
  - 90%：约 `26.964 → 29.713`
  - 95%：约 `23.619 → 26.909`

当前稳健状态：

```text
ROBUST_NUMERICAL
```

claim scope：

> 矩形联合区间上端点结果得到结构单调性与数值检查支持，但没有 interval BnB 严格证书。

因此图中不得出现：

```text
ROBUST_CERTIFIED
全局稳健最优
对所有自适应策略全局最优
```

---

# 4. 不列入本次“修图任务”的图片

以下图片不需要队友在这一轮继续修改：

| 图片 | 处理意见 | 原因 |
|---|---|---|
| **Q1-F1 主情景序贯验收决策边界** | 保留，进入最终 render check | 数据与图意符合正文需求 |
| **Q2 最优固定策略矩阵** | 保留 | 直接回答六种情形执行哪些动作，且保留情形3并列解 |
| **Q2 新版六类成本构成** | 保留 | 已把“拆解”与“调换损失”拆开，并正确保留情形3-A/3-B |
| **Q3-F1 Top5利润+相对损失** | 数据通过，仅按第2节核验标签 | 已达到正文需要的“最优策略及优势幅度”表达 |
| **Q4-F1 Q2追加抽样动作价值差** | 保留 | 直接解释当前12种演示情景为何均 STOP |
| **Q4-A1 联合置信区间** | 不修改，放附录 | 数据正确，但属于稳健不确定集构造的辅助图 |

### Q1-F4 特殊处理

`q1_4相同双风险约束下固定抽样与序列抽样的样本负担比较.pdf`

**不建议投入时间继续精修，正文直接淘汰或只保留其左面板重新利用。**

原因：

- 左面板 `n_F` vs `J_ASN` 有信息价值；
- 右面板在 `κ=1` 下比较 `n_F` 与 `N_max`，当前主方案基本重合，缺乏独立 claim；
- Q1-F2、Q1-F3 已经能够更完整地表达风险与效率。

若最终正文版面不足，优先删除 Q1-F4。

---

# 5. 队友修改顺序

建议按以下顺序处理：

1. **Q4-F2** —— 必须重画尺度，当前版式会直接损失信息。
2. **Q1-F2** —— 删除内部状态码、补风险线标签。
3. **Q4-F3** —— 明确 Uniform 主先验，并补结果元数据。
4. **Q3-F2** —— 修正文案与证据范围，不需重新计算。
5. **Q3-F1** —— 做最终策略位串渲染核验。

---

# 6. 提交回传要求

修改后请至少回传：

- 最新 Git commit SHA；
- 修改过的 `.m` 文件；
- 对应 PDF；
- 若修改结果元数据，同时提交更新后的 CSV/JSON；
- 简短 render-check 说明：
  - 中文字体正常；
  - 无裁切；
  - 图例不遮挡；
  - 标签不重叠；
  - 策略位串完整；
  - PDF 缩放后文字可读；
  - 灰度打印仍可区分主要编码。

**不要修改模型结果数值来适配图形。若绘图发现数据与预期不一致，应先报告 `SOURCE/RESULT MISMATCH`，不要在绘图脚本中手工覆盖结果。**
